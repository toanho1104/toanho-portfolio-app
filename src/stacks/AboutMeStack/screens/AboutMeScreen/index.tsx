import React from 'react';

import {DefaultLayout} from '@components/layout/defaultLayout';
import {MyText} from '@components/text';

interface IProps {}

export const AboutMeScreen = ({}: IProps) => {
  return (
    <DefaultLayout>
      <MyText>sfds</MyText>
    </DefaultLayout>
  );
};
